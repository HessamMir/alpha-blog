<h1 class = "text-center mt-4"> Listing Articles </h1>
  <%# pagination is for when the user or page has a lot of articles. it will allow us to
  go to different pages and not have all the articles in only one page. %>
<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div>

  <%= render 'article' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @articles, :container => false %>
</div>




