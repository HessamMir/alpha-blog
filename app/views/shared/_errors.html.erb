<% if obj.errors.any? %>
  <div class="alert alert-danger alert-dismissible fade show" role="alert">
  <%#  here i have put an error statement for the object class which can be article or user downcase %>
    <h4 class="alert-heading">the following errors prevented the <%= obj.class.name.downcase %> from being saved</h4>
    <ul>
    <%# after that the object errors will be displayed in full as a list  %>
    <% obj.errors.full_messages.each do |msg| %>
    <li > <%= msg  %> </li>
    <% end %>
    </ul>
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <h2 class = "text-center mt-4"> the following errors prevented the article from being saved </h2>

<% end %>