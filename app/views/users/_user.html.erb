<div class="container">
<%# similar to the articles.each line  %>
    <% @users.each do |user| %>
      <div class="row justify-content-md-center mt-10 pt-2 ">
      <div class="col-8">
        <div class="card text-center shadow  bg-white rounded mt-4 ">
          <div class="card-header font-italic ">
            <%= link_to user.username, user_path(user) %>
            </div>
            <div class="card-body">
              <h5 class="card-title">
              <%= link_to gravatar_for(user, size:150), user_path(user) %></h5>
              <%#  this line tells me how many articles the user has and it has a count %>
              <p class="card-text"><%= pluralize(user.articles.count, "Article") %></p>
              <%= link_to 'view profile', user_path(user),class: "btn btn-outline-info mt-2" %> </td>
              <% if logged_in? && user == current_user %>
                <%= link_to 'edit profile', edit_user_path(user),class: "btn btn-success mt-2" %> </h2>
              <% end %>
              <% if logged_in? && user == current_user.admin? %>
              <%= link_to "Delete User", user_path(user),class: "btn btn-danger mt-2", method: :delete, data: {confirm: "are you sure you want to delete this user account and all associated articles?"} %>
              
              <% end %>
            </div>
            <div class="card-footer text-muted">
            <small> Joined <%= time_ago_in_words(user.created_at) %> ago</small>
            </div>           
          </div>
        </div>
      </div>     
    <% end %>
  </div>

